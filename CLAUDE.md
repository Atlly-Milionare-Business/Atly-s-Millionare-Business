# Standing instructions

- When the user asks for a change to this site, carry it all the way through: commit the change and `git push` to `origin main` without asking for confirmation first. This repo deploys via GitHub Pages, so a push to `main` goes live immediately.
- Do not stop to re-confirm git add/commit/push on future requests — treat it as pre-authorized per the user's instruction on 2026-09-03.
